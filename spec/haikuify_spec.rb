require 'spec_helper'

describe Haikuify do
  xit 'has a version number' do
    expect(Haikuify::VERSION).not_to be nil
  end

  xit 'does something useful' do
    expect(false).to eq(true)
  end
end
